[project]
name = "example-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "firecrawl-py>=4.13.0",
    "google-generativeai>=0.8.6",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
